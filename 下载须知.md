# 下载须知

本插件（**抢票排队监控助手**）是一个 Chrome / Edge 浏览器扩展，**非官方工具，与任何票务平台无隶属关系**。

本仓库只提供源码与打包好的扩展文件夹，你需要自己把它「加载」到浏览器里使用，方法比从应用商店一键安装稍多一点，但全程免费、无需审核。下面三步即可用起来。

---

## 一、下载文件

1. 进入本项目的 GitHub 仓库页面。
2. 点击右上角的 **Code（绿色按钮）→ Download ZIP**，把压缩包下载到本地。
3. 解压后得到一个文件夹（通常名为 `nol-queue-monitor` 或 `nol-queue-monitor-main`）。**记住这个文件夹的位置**，下一步要用。

> 如果你会用 Git，也可以 `git clone` 本仓库，效果相同。

---

## 二、加载到浏览器（Chrome 与 Edge 操作几乎一样）

### Chrome

1. 在地址栏输入 `chrome://extensions` 并回车。
2. 打开页面**右上角的「开发者模式」**（Developer mode）开关。
3. 点击左上角的 **「加载已解压的扩展程序」**（Load unpacked）。
4. 选择**第一步解压出来的整个文件夹nol-queue-monitor-main**，确认。
5. 浏览器工具栏出现插件图标，即表示加载成功。

### Edge

1. 在地址栏输入 `edge://extensions` 并回车。
2. 打开页面**左下角的「开发人员模式」**开关。
3. 点击 **「加载已解压的扩展程序」**。
4. 选择解压出来的文件夹nol-queue-monitor-main，确认。
5. 工具栏出现插件图标，即表示加载成功。

> 提示：以后代码有更新时，回到这个页面点击插件卡片上的 **「重新加载」**（↻ 图标）即可，无需重复加载。

---



## 三、配置与使用

1. **准备飞书通知（推荐，但非必须）**
  - 在飞书里新建一个只有你自己的群 → 群设置 → 群机器人 → 添加「自定义机器人」→ 复制它的 **Webhook 地址**。
  - 点击浏览器工具栏的插件图标，点 **「修改设置」**，把 Webhook 地址粘贴进去，点 **「保存设置」**。
  - 可先点 **「发送测试通知」**，确认飞书群里收到了消息。
2. **设置提醒方式**
  - 在弹窗里勾选你想要的提醒通道：
    - **飞书通知**：排队进展发到你的飞书群。
    - **播放浏览器铃声**：电脑本地响铃（无需飞书）。
    - **浏览器通知**：系统通知弹窗。
    - **起床铃**：循环播放响亮闹钟声，并在页面上显示「停止起床铃」按钮，直到你手动停止（适合半夜叫醒熟睡的人，请确保电脑音量已开、标签页未静音）。
3. **设置提醒阈值**
  - 在「提醒阈值：前方人数 ≤」处填写数字（例如 500），达到该人数时触发提醒。
  - 可添加多个阈值（最多 5 个），每个阈值只提醒一次。
  - 点 **「保存设置」**。
4. **开启监控**
  - 打开抢票网站的**排队页面**。
  - 在插件弹窗右上角打开 **「启用监控」** 开关。
  - 之后保持该排队页面**打开、不要刷新**（刷新会重置你的排队位置）。

---



## 四、注意事项

- **排队页面必须保持打开**：刷新或关闭会丢失排队位置，监控也会中断。
- **起床铃音量受系统限制**：插件无法突破系统/标签页音量上限，请确认电脑音量已开、该标签页未被静音。
- **不会自动更新**：从 GitHub 下载的是固定版本，作者更新后你需要重新下载并「重新加载」扩展才能用上新功能。
- **多语言支持**：插件已适配中文、英文、日文、韩文的排队页面文案。
- **调试面板**：插件弹窗底部有一个可折叠的「调试信息」面板，遇到「该提醒却没提醒」时展开它，能看到插件实际识别到的页面状态，方便排查。

---



## 常见问题

**Q：加载时报错“找不到 manifest.json”？**
A：你选错了文件夹。请确认选中的是**包含** `manifest.json` **的那一层**文件夹，而不是它的上一级或压缩包本身。

**Q：明明人数到了阈值却没提醒？**
A：展开弹窗底部的「调试信息」面板，看 `isQueuePage` 是否为 `true`、`data` 里人数是否正常提取；把面板内容截图反馈，便于定位问题。

**Q：飞书收不到通知？**
A：检查 Webhook 地址是否完整粘贴、自定义机器人是否被停用或移出群聊、飞书群是否被设置为免打扰。

**Q：和浏览器应用商店的版本有区别吗？**
A：功能完全一致。本仓库版本只是没有经过商店审核，需要你手动加载，好处是免费、即时可用、不被下架。
